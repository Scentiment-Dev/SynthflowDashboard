# Local Config

This folder contains non-secret local configuration templates for development. Do not store credentials here.

`dashboard.local.json` documents local URLs, feature flags, and source-of-truth ownership used by the skeleton app.
