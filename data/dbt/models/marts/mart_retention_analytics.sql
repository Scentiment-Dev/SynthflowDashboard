select
    journey_id,
    call_id,
    customer_id,
    subscription_id,
    canonical_intent,
    flow_branch,
    cancel_reason,
    journey_started_at,
    journey_last_event_at,
    frequency_offer_presented,
    discount_offer_presented,
    frequency_offer_accepted,
    discount_offer_accepted,
    frequency_offer_declined,
    discount_offer_declined,
    is_cost_too_high_path,
    cost_too_high_sequence_valid,
    save_confirmed,
    cancelled_confirmed,
    has_stayai_confirmation,
    final_retention_outcome,
    trust_label,
    formula_version,
    calculated_at
from {{ ref('fact_cancellation_retention_journey') }}
